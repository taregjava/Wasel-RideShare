spring:
  application:
    name: wasel-location
  data:
    redis:
      host: localhost
      port: 6379
#      timeout: 2000ms
#      password: ${REDIS_PASSWORD:}

management:
  endpoints:
    web:
      exposure:
        include: "health", "info", "metrics", "prometheus"
